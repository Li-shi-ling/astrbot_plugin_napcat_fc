{
  "debug": {
    "description": "是否启用调试日志。开启后插件运行到的关键节点会通过 AstrBot logger.debug 输出。",
    "type": "bool",
    "hint": "排查工具搜索、工具调用、搜索屏蔽队列、数据库同步问题时开启；正常使用建议关闭。",
    "default": false
  },
  "admin_only": {
    "description": "是否仅允许 AstrBot 管理员使用 NapCat 搜索和调用工具。",
    "type": "bool",
    "hint": "默认开启。开启后 napcat_search_tools 和 napcat_call_tool 会通过 event.is_admin() 校验调用者；关闭后非管理员也可以使用这两个入口，请确认风险后再关闭。",
    "default": true
  },
  "search_candidate_limit": {
    "description": "NapCat 搜索工具每次参与综合排序的候选工具数量。",
    "type": "int",
    "hint": "默认 10。调大可以让被近期屏蔽的工具跳过后仍有更多新工具进入候选，但会增加少量数据库搜索和排序开销。最小有效值为 1。",
    "default": 10
  },
  "search_result_format": {
    "description": "NapCat 搜索工具的结果格式。pipe 和 tsv 为完整文本调用说明，json 为完整结构化 JSON。",
    "type": "string",
    "hint": "默认 pipe。pipe 和 tsv 会返回每个候选工具的用途、风险信息、完整参数说明、必填参数、调用样例和 napcat_call_tool 使用说明；json 返回完整结构化对象。可选值：pipe、tsv、json。",
    "default": "pipe"
  },
  "search_result_suppress_turns": {
    "description": "NapCat 搜索工具返回过的工具在同一会话内被临时排除搜索的对话轮数。",
    "type": "int",
    "hint": "默认 3。搜索结果返回过的工具会在接下来的 N 次 LLM 请求中不再被搜索到，避免重复返回同一批工具；小于等于 0 表示直到聊天历史清空前都不再搜索到这些工具。插件会根据 req.contexts 长度回退判断聊天历史被清理并自动重置队列。",
    "default": 3
  },
  "fallback_invalid_context_ids": {
    "description": "是否在 group_id、user_id、self_id 等上下文 ID 参数小于 6 位或不是纯数字时，自动回退为当前会话默认值并输出警告日志。",
    "type": "bool",
    "hint": "默认开启。用于容错 LLM 误传 0、短 ID 或非法 ID，防止明显错误的目标 ID 被原样发送给 NapCat。",
    "default": true
  }
}
