{
  "base_url": {
    "type": "string",
    "description": "NapCat OneBot HTTP 地址，例如 http://127.0.0.1:3000",
    "default": "http://127.0.0.1:3000"
  },
  "access_token": {
    "type": "string",
    "description": "OneBot 访问令牌；为空时不发送 Authorization 头",
    "default": ""
  },
  "timeout": {
    "type": "float",
    "description": "接口请求超时时间，单位秒",
    "default": 30.0
  },
  "tool_prefix": {
    "type": "string",
    "description": "注册给 LLM 的函数工具名前缀",
    "default": "napcat"
  },
  "register_tools": {
    "type": "bool",
    "description": "是否在插件加载时注册 NapCat 函数工具",
    "default": true
  },
  "enable_generic_tool": {
    "type": "bool",
    "description": "是否额外注册通用调用工具 napcat_call_api",
    "default": true
  }
}
